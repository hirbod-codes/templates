export enum GraphNames {
    READS = "reads",
    UPDATES = "updates",
    DELETES = "deletes",
}

export enum EdgeCollectionNames {
    READS = "reads",
    UPDATES = "updates",
    DELETES = "deletes",
}

export enum DocumentCollectionNames {
    USERS = "users",
}

export default { DocumentCollectionNames, GraphNames, EdgeCollectionNames }
